

// 함수표현식 :
const Header = () => {
    return (
        <header className="Header">
            header
        </header>
    )
}

export default Header;

// 함수 선언 : function(){ return() }
// this를 사용할 수 있다는 장점.
// 어떤 방식이든 상관없지만 사용에 있어 통일성을 가져주는 것이 좋다.

